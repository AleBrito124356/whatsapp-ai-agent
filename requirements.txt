# Core web + HTTP
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.6

# LLM (NVIDIA NIM is OpenAI-compatible)
openai>=1.30

# IANA timezone database (needed on Windows for zoneinfo)
tzdata>=2024.1

# --- Optional: local voice-note transcription ---
# Uncomment to enable audio transcription. Requires ffmpeg on your PATH.
# faster-whisper>=1.0

# --- Dev / tests ---
pytest>=8.0
